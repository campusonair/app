import React from "react";

type Props = {
  // show: boolean;
  // src: {
  //   top: number;
  //   left: number;
  // };
  // dest: {
  //   top: number;
  //   left: number;
  // };
  // transition: number;
};

const DriftShip = () => {
  [];
};

const DriftIcon = (props: Props) => {
  // React.Animated
  // const { show } = props;
  return <i>{"❤️"}</i>;
};

export default DriftIcon;
